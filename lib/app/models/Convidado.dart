class Convidado {
  final int id;
  final String nome;
  final bool isPadrinho;
  final int id_evento;

  const Convidado({
    required this.id,
    required this.nome,
    required this.isPadrinho,
    required this.id_evento
  });
}